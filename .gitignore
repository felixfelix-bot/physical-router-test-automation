node_modules/
test-results/
report/
playwright-report/
test-run-*
.DS_Store
*.ipk
scripts/mint-token/mint-token
.env

# Python
__pycache__/
*.pyc
_cashu_db/
/report.html
results/

# Router inventory (contains IPs, SSIDs)
config/routers.json

# Firmware build credentials (contains passwords)
credentials/

# Temporary work directories
.tmp/
findings/

# Agent orchestration artifacts
.sisyphus/
.omo/

# Playwright MCP session logs
.playwright-mcp/

# Hardware test environment files
**/routers.env
**/*.lock
hardware.lock
routers.lock

# Screenshots and test artifacts — never commit or publish these
*.png
*.jpg
*.jpeg
*.gif
*.webm
*.mp4
nuts/

# Self-hosted GitHub Actions runner installation (local, not source)
actions-runner/

# Experimental Sovereign Hybrid Compute (SHC) client — not yet integrated
lib/cloud_lab/shc.py
*.pyc
__pycache__/
test-videos*/

# Pulumi SHC spike (infra/pulumi-shc-spike/) — local-only artifacts
# Stack config can hold secrets if `pulumi config set --secret` is used;
# the spike deliberately reads SHC_API_KEY from env instead. State and
# logs are local-only regardless.
infra/pulumi-shc-spike/.venv/
infra/pulumi-shc-spike/.state/
infra/pulumi-shc-spike/Pulumi.*.yaml
infra/pulumi-shc-spike/last-*.txt
infra/pulumi-shc-spike/__pycache__/
