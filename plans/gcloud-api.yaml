name: gcloud-api
description: "Full API test suite against GCloud virtual lab"
matrix:
  lab: gcloud
  tier: api
  scope: full
runner: pytest
pytest:
  markers: "api and extended"
  testpaths: "tests/api"
playwright: null
env:
  TOLLGATE_VIRTUAL_LAB: "true"
  TOLLGATE_CLIENT_TYPE: container
