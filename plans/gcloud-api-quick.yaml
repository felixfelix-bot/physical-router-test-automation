name: gcloud-api-quick
description: "Quick API subset against GCloud virtual lab"
matrix:
  lab: gcloud
  tier: api
  scope: quick
runner: pytest
pytest:
  markers: "api and critical"
  testpaths: "tests/api"
playwright: null
env:
  TOLLGATE_VIRTUAL_LAB: "true"
  TOLLGATE_CLIENT_TYPE: container
