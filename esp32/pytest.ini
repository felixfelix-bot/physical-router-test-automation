[pytest]
testpaths = tests
markers =
    board_a: tests for Board A
    board_b: tests for Board B
    board_c: tests for Board C
    requires_funding: tests that need a funded wallet
    smoke: quick smoke tests (~30s)
addopts = -v --tb=short
